import { Col } from 'antd';
import styled from 'styled-components';

export const ProductsWrapper = styled.div`
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-top: 20px;
    flex-wrap: wrap;
`;

export const VerticleNavBarWrapper = styled(Col)`
    background: #fff;
    margin-right: 10px;
    padding: 10px;   
    border-radius: 6px;
    margin-top: 20px;
    height: fit-content;
`
